<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rogmax.amirjaber.taskrogmax22">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreatePetActivity" />
        <activity android:name=".EditPetActivity" />
        <activity android:name=".CreatePersonActivity" />
        <activity android:name=".ViewPeopleActivity"></activity>
    </application>

</manifest>